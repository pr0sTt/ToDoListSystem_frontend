
  # ToDo list app

  This is a code bundle for ToDo list app. The original project is available at https://www.figma.com/design/ZZF8DtrV8hyAclnPBDasLT/ToDo-list-app.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  